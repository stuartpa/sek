# `models/` — MDLxxx

SEK model definitions (state fields, actions, invariants) of the implementation's state space,
produced in **Stage 4 · Model** via `/speckit.engloopkit.model`. See [../standards.md](../standards.md).
